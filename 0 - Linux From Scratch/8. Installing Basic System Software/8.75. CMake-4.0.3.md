## 8.75.1. Installation of CMake

Prepare CMake for compilation:

```shell
./configure --prefix=/usr
```

Compile the package:

```shell
make
```

To test the results, issue:

```shell
make test
```

Install the package:

```shell
make install
```